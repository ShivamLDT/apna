# # """
# # This script runs the FlaskWebProject3 application using a development server.
# # """
# from flask_socketio import SocketIO
# #from FlaskWebProject3 import app
# #sktio = SocketIO(cors_allowed_origins="*")
# # #sktio = SocketIO(cors_allowed_origins="*",wait=10,logger=True ,engineio_logger=True,ping_timeout=20, ping_interval=25)
# #sktio = SocketIO(app,cors_allowed_origins="*")#,async_mode='threading', logger=True ,engineio_logger=True,ping_timeout=20, ping_interval=25)
# #global sktio = None#SocketIO(cors_allowed_origins="*")#,async_mode='threading', logger=True ,engineio_logger=True,ping_timeout=20, ping_interval=25)
